<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Gradify" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Gradify" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/appColor</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/white</item>
        <item name="tint">@color/white</item> <!-- Set your desired tint color here -->
        <item name="postSplashScreenTheme">@style/Base.Theme.Gradify</item>
    </style>

    <style name="CustomBottomNavigationItem" parent="Widget.Design.BottomNavigationView">
        <item name="android:textSize">12sp</item> <!-- Change to your desired font size -->
        <item name="fontFamily">@font/medium</item>
    </style>
</resources>